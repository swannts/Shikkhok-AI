/**
 * User roles in the Shikkhok-AI platform.
 * Each role defines a distinct authorization scope.
 */
export enum UserRole {
  STUDENT = 'student',
  PARENT = 'parent',
  TEACHER = 'teacher',
  ADMIN = 'admin',
}
